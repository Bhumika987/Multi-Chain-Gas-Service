services:
  - type: web
    name: gas-tracker
    runtime: python
    pythonVersion: "3.9.16"  # Explicitly locked
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 8000"